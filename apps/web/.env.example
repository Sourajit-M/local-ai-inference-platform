# API base URL — set this on Vercel if you have a deployed backend
# Leave blank to default to http://localhost:8000/api
VITE_API_URL=

# Set to "true" on Vercel to always show demo data (no backend required)
VITE_DEMO_MODE=false
